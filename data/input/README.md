# Raw Input Data

Place raw CSV exports from Excel in this folder.

Recommended filename for the OCP Benguerir annual PV profile:

```text
data/input/ocp_profile.csv
```

Do not store large real client datasets in source control unless explicitly approved. The backend scripts can read CSV files from this folder and write normalized reporting outputs to `data/processed`.

